services:
  web:
    build: 
      context: ..
      dockerfile: docker/Dockerfile.web
    ports:
      - "${APP_PORT}:5000"

  worker:
    build:
      context: ..
      dockerfile: docker/Dockerfile.worker
    command: python jobs.py